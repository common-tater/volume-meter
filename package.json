{
  "name": "volume-meter",
  "version": "2.0.1",
  "description": "Renders the dynamic range of a MediaStream waveform slice as a percentage in a requestAnimationFrame callback",
  "keywords": "MediaStream volume meter dynamic range",
  "devDependencies": {
    "browserify": "^8.1.3",
    "ecstatic": "^0.5.8",
    "getusermedia": "^1.2.0"
  },
  "scripts": {
    "example": "browserify example > example/build.js && node example/server.js"
  },
  "repository": "https://github.com/jessetane/volume-meter",
  "author": "Jesse Tane <jesse.tane@gmail.com>",
  "dependencies": {
    "raf": "^3.1.0"
  }
}
